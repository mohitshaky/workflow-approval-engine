
apiVersion: v1
kind: Service
metadata:
  name: workflow-backend-service
spec:
  selector:
    app: workflow-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP